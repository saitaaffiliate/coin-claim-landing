# Optional: forward stub API routes to a real game server
# GAME_SERVER_URL=https://your-game-api.example.com

# Optional: browser fetch prefix (leave unset to use this app's /api)
# NEXT_PUBLIC_API_BASE=
